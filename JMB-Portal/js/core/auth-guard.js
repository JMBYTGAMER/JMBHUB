import {auth,firebaseEnabled} from './firebase.js'; import {onAuthStateChanged,signOut} from 'https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js'; import {APP_CONFIG} from '../config/app-config.js'; import {$,setText} from './ui.js';
export function guard(){if(!firebaseEnabled){setText('userName','Demo User');setText('userEmail','Firebase not configured');return Promise.resolve({uid:'demo',email:'demo@jmb.local',displayName:'Demo User',demo:true})}return new Promise(resolve=>{onAuthStateChanged(auth,u=>{if(!u){location.href='login.html';return}setText('userName',u.displayName||u.email?.split('@')[0]||'JMB User');setText('userEmail',u.email||'');const av=$('#userAvatar');if(av)av.textContent=(u.displayName||u.email||'J')[0].toUpperCase();const owner=u.email?.toLowerCase()===APP_CONFIG.OWNER_EMAIL.toLowerCase();$$owner(owner);resolve({...u,owner})},{onlyOnce:false})})}
function $$owner(owner){document.querySelectorAll('[data-owner-only]').forEach(e=>e.classList.toggle('hide',!owner))}
export async function logout(){if(firebaseEnabled)await signOut(auth);location.href='login.html'}
