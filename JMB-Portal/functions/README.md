# Optional Firebase backend templates

GitHub Pages cannot run server-side code. Put secure OAuth and privileged operations in Firebase Cloud Functions or another backend.

Use this folder as a template for:
- Discord OAuth callback
- Firebase custom claims for admin roles
- Secure ticket reply actions

Do not deploy code containing hard-coded secrets.
